# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-5.3.1
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2025-02-28T19:45:20Z"
  labels:
    app: nginx
  name: nginx
spec:
  containers:
  - args:
    - nginx
    - -g
    - daemon off;
    env:
    - name: TERM
      value: xterm
    image: docker.io/library/nginx:alpine
    name: nginx
    ports:
    - containerPort: 80
      hostPort: 9000
    stdin: true
    tty: true
